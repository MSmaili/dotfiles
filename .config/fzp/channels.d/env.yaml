metadata:
  description: Environment variables

source:
  command: env | sort
  delimiter: "="

preview:
  command: echo {} | cut -d= -f2-
  window: right:40%

ui:
  header: env

actions:
  f2:
    mode: execute-silent
    label: copy value
    command: echo -n {} | cut -d= -f2- | pbcopy
